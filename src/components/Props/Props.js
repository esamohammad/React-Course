import React from 'react';



const Props = (props) => {
    return (
        <div>
            <h3>{props.titleText} and and call him {props.description}</h3>
        </div>
    );
};

export default Props;